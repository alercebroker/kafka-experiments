apiVersion: 1

providers:
  - name: "Kafka Overview"    # A uniquely identifiable name for the provider
    folder: Kafka
    type: file
    options:
      path:  /var/lib/grafana/dashboards

  - name: "Node Exporter"
    folder: NodeExporter
    type: file
    options:
      path: /var/lib/grafana/dashboards
